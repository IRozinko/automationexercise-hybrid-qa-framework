[pytest]
addopts =
    --alluredir=allure-results
testpaths = tests
markers =
    e2e: end-to-end tests
    ui: browser-driven UI tests
    api: API-backed setup and assertions
    smoke: critical path smoke tests
